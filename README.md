# Builder Codesheet — pre-test

A 25-minute coding exercise: read two definitions, answer three checks, code 20 products,
give feedback. Open `index.html` (or the GitHub Pages URL for this repository).

No account is needed. Results are sent to the study's collector on Submit; if that cannot
be reached, the page shows your answers to copy and send instead.
